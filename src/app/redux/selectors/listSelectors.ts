// export const getLists = (store) => {
//     return store && store.lists ? store.lists.allIds : [];
// }

// export const getListById = (store, id) => {
//     return store && store.lists && store.lists.byIds ? {...store.lists.byIds[id], id} : {};
// }
export {}