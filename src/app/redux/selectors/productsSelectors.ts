// export const getAllProducts = (store) => {
//     return store && store.products ? store.products.allIds : [];
// }

// export const getProductById = (store, id) => {
//     return store && store.products && store.products.byIds ? {...store.products.byIds[id], id} : {};
// }

// export const getProductByCategory = (store, category) => {
//     return store && store.products && store.products.category ? {...store.products.category} : {};
// }

// export const getPopularProducts = (store, popularFilter) => {
//     return store && store.products && store.products.popularFilter ? {...store.products.popularFilter} : {};
// }
export {}